internal class Program
{
    private static void Main(string[] args)
    {
        int DAY = 1
switch (DAY)
{
    case 1:
        // Statements executed if expression(or variable) = value1
        Console.WriteLine("NULL")
        break;
    case 2:
        // Statements executed if expression(or variable) = value1
        break;
    /*
    */
    default:
        // Statements executed if no case matches
        Console.WriteLine("ERRDEF")
}
    }
}