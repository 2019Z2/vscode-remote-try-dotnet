

int [] grades = {100, 80 , 90, 70};


int Avg = Avarage(grades);

int Avarage(int[] grades)
{
    int sum = 0;
    int avg =0 ;
    for(int i = 0; 1 < grades.Length; i++)
    {
        sum += grades{i}
    }
    avg = sum / 4;
    return avg;

}
